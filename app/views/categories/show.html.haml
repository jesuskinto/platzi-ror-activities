.category.show-page
  .card
    .card-body
      %p
        %b Name:
        = @category.name
      %p
        %b Description:
        = @category.description
      = link_to t('common.edit'), edit_category_path(@category), class: 'btn-interaction'
      \|
      = link_to t('common.back'), categories_path, class: 'btn-interaction'